# 测试报告：P0-5 修复硬编码 Linux 绝对路径（RISK-1）

> 日期：2026-05-13
> 任务：P0-5 修复硬编码 Linux 绝对路径
> 版本：v4.2.0-dev
> 执行者：Developer

## 变更范围

| 文件 | 变更 |
|------|------|
| `src/index.js` | `baseDir` 从 `'/root/.agent'` 改为 `process.env.OPENCLAW_AGENT_DIR \|\| join(homedir(), '.agent')` |
| `src/common/adapters/state.js` | `DEFAULT_DIR` 使用 `homedir()` + 环境变量覆盖 |
| `src/common/adapters/memory.js` | `DEFAULT_DB` 使用 `homedir()` + 环境变量覆盖 |
| `src/common/adapters/flow.js` | 默认 `dbPath` 使用 `homedir()` + 环境变量覆盖 |
| `src/common/adapters/hook.js` | `DEFAULT_HOOKS_DIR` 使用 `homedir()` + 环境变量覆盖 |
| `src/common/adapters/task.js` | 默认 `dbPath` 使用 `homedir()` + 环境变量覆盖 |

## 验证结果

- **硬编码残留检查**：`grep -r '/root/\.agent' src/` → 0 处业务代码残留，仅注释提及
- **向后兼容**：`options.dir` / `options.dbPath` 传入时仍优先使用
- **全量测试**：120/120 通过，0 失败

## 测试摘要

```
# tests 120
# suites 45
# pass 120
# fail 0
# duration_ms 5132
```

---

[TEST_PASS] [DOC_UPDATE: TODO]
