# 测试报告：P0-3 案例索引数据库

> 日期：2026-05-13
> 任务：P0-3 案例索引数据库
> 版本：v4.2.0-dev
> 执行者：Developer

## 变更范围

| 文件 | 变更 |
|------|------|
| `src/common/case-index.js` | 新增：SQLite 案例索引 + Jaccard 关键词相似度 |
| `src/index.js` | 创建 `CaseIndex` 实例并注入 tool deps |
| `src/tools/metacognition-tools.js` | `create_plan` 返回 `similarCases` |
| `src/tools/working-memory-tools.js` | `archive_task` 调用 `caseIndex.indexTask` |

## 相似度验证

```javascript
// 索引3个案例后查询
await ci.findSimilarCases('修复硬编码路径', 3);
// → case-1 relevance: 0.64（强相关）
// → case-3 relevance: 0.31（弱相关）
// → case-2 未返回（无关，自动过滤）
```

- 查询时间：< 5ms（远小于 200ms 标准）
- 自动清理：超过 `maxArchivedTasks` 时删除最旧记录

## 验证结果

- **全量测试**：120/120 通过，0 失败
- **向后兼容**：`caseIndex` 为可选依赖，未注入时不影响流程

## 测试摘要

```
# tests 120
# suites 45
# pass 120
# fail 0
# duration_ms 5255
```

---

[TEST_PASS] [DOC_UPDATE: TODO]
