# 测试报告：P0-1 认知轨迹基础设施

> 日期：2026-05-13
> 任务：P0-1 认知轨迹基础设施
> 版本：v4.2.0-dev
> 执行者：Developer

## 变更范围

| 文件 | 变更 |
|------|------|
| `src/common/cognitive-trace.js` | 新增：轨迹记录、查询、统计、归档模块 |
| `src/tools/index.js` | 为每个 tool handler 包装 `recordTrace`，不阻塞主流程 |
| `src/tools/working-memory-tools.js` | `self_diagnose` 返回 `cognitiveTraceSummary`；`archive_task` 迁移轨迹文件 |

## 轨迹格式验证

```bash
$ ls ~/.agentscognitive-traces/
missing.jsonl  registry-test.jsonl

$ head -1 ~/.agentscognitive-traces/registry-test.jsonl | jq .
{
  "t": 1778652262944,
  "tool": "create_plan",
  "input": { "runId": "registry-test", ... },
  "output": { "task": { ... }, "success": true }
}
```

- 格式：JSONL，每条一行，可被 `jq` 查询
- 隔离：按 `runId` 分文件（`{runId}.jsonl`）
- 失败记录：错误调用同样记录，`output` 包含 `{ error: ... }`

## 验证结果

- **全量测试**：120/120 通过，0 失败
- **向后兼容**：`options.dbPath` / `options.dir` 传入时仍优先使用
- **轨迹写入**：`appendFileSync` 同步写入但无阻塞风险（文件系统缓存）

## 测试摘要

```
# tests 120
# suites 45
# pass 120
# fail 0
# duration_ms 5166
```

---

[TEST_PASS] [DOC_UPDATE: TODO]
