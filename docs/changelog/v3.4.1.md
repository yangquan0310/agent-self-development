# v3.4.1 — 2026-05-04

## Added
- Task lifecycle logging (JSONL format) across all modules
- `Stream` module for `llm_output` chunk buffering and aggregation
- Injection fatigue protection (`_injectCount` tracking)

## Changed
- Skill injection instructions strengthened for better Agent compliance
- `Skills.load()` now logs ERROR instead of silently skipping on failure

## Fixed
- Skill load failure silently skipped
- Version number inconsistency across skill frontmatter
- Duplicate skill injection on retries
