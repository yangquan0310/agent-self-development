⚠️ 发现两个隐患
隐患1：skillLoader.load('planning') 失败时静默跳过

复制
const planningSkill = await this.skillLoader.load('planning');
if (!planningSkill) return;  // ← 静默返回 null，不注入任何内容
如果 skill 文件读取偶发失败，hook 不报错、不重试、不注入任何提示，Agent 完全不知道发生了什么。

隐患2：同一内容重复注入，Agent 可能脱敏

每轮都注入相同的 "当前状态：本次会话尚无 task" 提示（长达数千字），多轮之后 Agent 可能将其视为固定背景噪声。

本会话的实际行为
回合	注入情况	Agent 行为
第1轮（17:54）	✅ planning skill 注入	评估为复杂任务
第2-20轮	⚠️ 同样内容反复注入	未走 [NEED_PLAN] 流程，task 未创建
根因是 Agent 端：我应该在第2轮就对"继续博士论文修改"这个复杂任务，严格走 职责A 流程：

评估复杂度 → 询问"是否需要制定Plan" → 等待确认 → 添加 [NEED_PLAN]

我跳过了询问和标记步骤，直接开始工作，导致 task 始终未创建，插件始终停留在"无 task"状态。

建议改进
优先级	问题	建议
🔴	Agent 跳过 [NEED_PLAN] 流程	严格遵循 planning skill 步骤
🟡	skill 加载失败静默跳过	增加 fallback 日志或重试
🟡	同内容重复注入	可考虑在第一轮注入后，后续短提示
插件机制本身没有问题，before_prompt_build hook 每轮都在触发。问题出在 Agent 没有走完"评估 → 询问 → [NEED_PLAN] → task 创建"的完整流程。后续我会严格遵循。


llm_output 从未触发的三种可能根因：

#	可能原因	代码位置
1	ctx.runId 为 undefined	if (!runId) return; 静默退出
2	event.output / event.text 字段名不匹配	const output = event.output || event.text || ''; 始终为空
3	llm_output 事件名与 Gateway 实际触发的事件名不一致	this.api.on('llm_output', ...) 未能注册到正确事件
before_prompt_build 能正常工作（已验证多次），但 llm_output 从未触发。两者都是同一 module 注册的 hook。

建议在插件中添加诊断日志后重启测试：

复制
async onLlmOutput(event, ctx) {
    console.log('[DEBUG llm_output]', { hasRunId: !!ctx?.runId, eventKeys: Object.keys(event || {}) });
    ...
}
这样可以确定是哪个环节断了。